📂 Project Status: Tujen Bot v4 + Recombinator
✅ Реализованный функционал
1. Ядро (Core)

Inventory Manager: Потокобезопасное хранение состояния слотов (60 слотов). Статусы предметов (CRAFT_BASE, FINISHED, TRASH, LOCKED, OCR_ERROR).

Input Handler: Человекоподобные движения мыши (кривые Безье), поддержка MOUSE_DRIFT (случайное смещение координат), безопасное нажатие клавиш.

Config System: Разделение на технический конфиг (config.py) и логический конфиг модов (mods_config.py).

2. Перцепция (Perception)

Scanner (Clipboard): Анализ предметов через Ctrl+Alt+C.

Парсинг классов предметов.

Поиск T1 модов по словарю.

Очистка мусора (диапазоны значений, лишние символы).

Защита от "фантомных копий" (очистка буфера перед копированием).

OCR Engine: Распознавание текста на экране (Tesseract). Используется для проверки результата крафта и наличия модов в окне рекомбинатора.

Vision: Быстрое определение занятых слотов (попиксельный анализ).

3. Логика (Logic)

Smart Matchmaker: Подбор пар для крафта.

Исключение пересекающихся тегов (не скрещиваем одинаковое).

Учет групп исключений (Mutually Exclusive), например, не скрещиваем Mana и Life на амулетах, если так задано.

Автоматическое определение готовых предметов (2+ тега).

4. Исполнители (Executors)

Recombinator Executor:

Полный цикл: Скан -> Матчмейкинг -> Перенос -> Крафт -> Проверка.

Error Handling: Если OCR не находит мод в окне рекомбинатора — процесс прерывается, предметы возвращаются в инвентарь, помечаются как OCR_ERROR.

Safety: Проверка "Failed" сообщения, проверка яркости результата. Удаление предметов из памяти только после подтверждения результата.

Shopping Executor (Tujen): Базовая покупка валюты/карт (на основе Regex).

🗺️ Roadmap (Планы развития)
1. Тестирование и стабилизация Recombinator 🛠️
Задача: Прогнать длительные циклы крафта (50-100 пар).

Цель: Выявить редкие баги (зависания анимаций, ложные срабатывания OCR, вылеты при пустом инвентаре).

Действие: Логирование всех ошибок и фиксы.

2. Умный Шоппинг (Clipboard Analysis for Tujen) 🧠
Проблема: Игровой поиск (Regex) имеет лимит 50 символов. Сложно искать комбинации (например, "Boots with 30% MS + 80 Life + Res").

Решение:

Кликать по каждому предмету у Туджина (или наводить мышь).

Делать Ctrl+Alt+C.

Анализировать через Python (полная свобода условий: веса статов, точные значения, комбинации).

Покупать только то, что прошло фильтр.

3. Humanization & Drift 🖱️
Задача: Сделать движения мыши в Рекомбинаторе (и Шоппинге) еще более "живыми".

Детали:

Внедрить drift_radius (который мы добавили в код) во все клики экзекьюторов.

Добавить микро-паузы и "паразитные" движения (иногда мышь дергается чуть в сторону).

Рандомизация точек клика по кнопкам (не в центр, а по площади кнопки).

4. Разгрузка инвентаря (Stashing) 📦
Задача: Когда инвентарь заполняется FINISHED предметами или мусором.

Решение:

Подход к стэшу.

Выгрузка готовых предметов (Ctrl+Click).

(Опционально) Вендор мусора.

5. Currency Exchange (Faustus) 💰
Задача: Автоматизация обмена валюты на бирже.

Потенциал:

Снайпинг выгодных курсов.

Автоматическая закупка ресурсов для крафта (например, золото для рекомбинатора, если в PoE 2 это актуально, или просто валюта).